#import "./UserGroupFrag.gql"

subscription addedToUserGroup($user: ID!) {
  authCreated(user: $user) {
    _id
    role
    shared_resource {
      ... on UserGroup {
        ...UserGroup
      }
    }
  }
}
